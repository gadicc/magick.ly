import { pageMetadata } from "@/seo/metadata";
import GeomancyReference from "./reference";

export const metadata = pageMetadata("/geomancy/reference");

export default function GeomancyReferencePage() {
  return <GeomancyReference />;
}
